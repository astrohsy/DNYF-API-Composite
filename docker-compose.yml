version: '3.7'

services:
  api:
    image: dnyf-composite-api
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      UVICORN_PORT: "8010"
    ports:
      - 8010:8010
    volumes:
      - ./:/app
